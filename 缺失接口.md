# 缺失接口说明

根据 `接口文档v2.md` 与当前前端页面能力对照，以下接口在后端文档中未提供，或现有接口不足以完整支撑页面回显，前端已做预留处理。

## 1. 工作台聚合接口

- 建议路径：`GET /api/v1/dashboard/summary`
- 当前前端预留位置：`src/api/analysis.js` 的 `getDashboardSummary`
- 当前处理方式：
  - 优先尝试用现有接口聚合：
    - `GET /api/v1/members`
    - `GET /api/v1/consumptions`
    - `GET /api/v1/consumptions/recent`
    - `GET /api/v1/levels`
    - `GET /api/v1/member-coupons`
  - 若聚合失败，则返回前端预留空结构，保证工作台逻辑不变
- 建议返回字段：
  - `totalMembers`
  - `totalRevenue`
  - `totalPoints`
  - `totalIssuedCoupons`
  - `totalUsedCoupons`
  - `recentRecords`
  - `lineData`
  - `pieData`

## 2. 管理员角色回显接口

- 当前已存在写接口：`PUT /api/v1/admins/{id}/roles`
- 缺失内容：管理员当前已分配角色的查询能力
- 影响页面：管理员管理页的“分配角色”弹窗无法准确回显当前角色，只能提交新分配结果
- 建议补充方案之一：
  - `GET /api/v1/admins/{id}/roles`
- 建议返回字段：
  - `roleIds: string[]`

## 3. 角色权限回显接口

- 当前已存在写接口：`PUT /api/v1/roles/{id}/permissions`
- 缺失内容：角色当前已分配权限的查询能力
- 影响页面：角色管理页的“分配权限”弹窗无法回显已勾选权限树
- 建议补充方案之一：
  - `GET /api/v1/roles/{id}/permissions`
- 建议返回字段：
  - `permissionIds: string[]`

## 4. 生命周期阶段分布/漏斗明细接口

- 当前文档已有：`GET /api/v1/analysis/lifecycle`
- 当前文档返回：
  - `newTrend`
  - `activeCount`
  - `lostCount`
  - `totalMembers`
- 缺失内容：
  - 生命周期阶段饼图明细
  - 生命周期漏斗图明细
- 当前前端处理方式：
  - 保持原生命周期页面逻辑不变
  - 使用现有返回值在前端做推导与预留数据组装
- 若后端希望与前端图表完全对齐，建议补充：
  - `distribution`
  - `funnel`

## 5. 消费偏好画像接口

- 当前文档已有：
  - `GET /api/v1/analysis/behavior/order-amount`
  - `GET /api/v1/analysis/behavior/repurchase`
  - `GET /api/v1/analysis/behavior/time-distribution`
- 缺失内容：
  - 消费偏好雷达图所需的画像数据
- 影响页面：消费行为分析页中的雷达图目前使用前端预留数据
- 建议补充方案之一：
  - `GET /api/v1/analysis/behavior/preference-radar`
- 建议返回字段：
  - `male: number[]`
  - `female: number[]`

## 6. 会员优惠券联表展示字段

- 当前文档 `GET /api/v1/member-coupons` 返回字段不包含：
  - 会员姓名
  - 优惠券名称
- 当前前端处理方式：
  - 列表页仅展示 `couponId`
  - 会员名称通过会员列表做本地映射
- 若后端希望减少前端二次查询，建议补充以下任一方式：
  - 在 `MemberCouponVo` 中增加 `memberName`、`couponName`
  - 或补充 `GET /api/v1/member-coupons/{id}` 明细联表字段

## 7. 消费记录联表展示字段

- 当前文档 `GET /api/v1/consumptions` 返回字段不包含：
  - 会员姓名
  - 门店名称
- 当前前端处理方式：
  - 通过会员列表、门店列表分别做本地映射
- 若后端希望减少前端额外请求，建议在 `ConsumptionVo` 中补充：
  - `memberName`
  - `storeName`

---

以上缺口中，前端已经完成“预留”或“兼容聚合”处理，不会阻塞当前页面开发；但若后端后续补齐，建议优先处理第 1、2、3、5 项，这几项对工作台与权限管理体验影响最大。
