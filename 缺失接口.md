# 缺失接口说明

根据最新版 [接口文档.md](D:/files/Code/GraduationProject/bellego-member-smart/接口文档.md) 与当前前端页面能力对照，当前仍建议后端补充或明确的接口如下。

## 1. 工作台专用聚合接口

- 建议路径：`GET /api/v1/dashboard/summary`
- 当前前端位置：`src/api/analysis.js` 中的 `getDashboardSummary`
- 当前处理方式：
  - 前端通过已有接口聚合工作台数据：
    - `GET /api/v1/members`
    - `GET /api/v1/analysis/behavior/daily-consume`
    - `GET /api/v1/analysis/behavior/level-count`
    - `GET /api/v1/member-coupons`
    - `GET /api/v1/consumptions`
  - 这样已经可用，但会带来多次请求与前端聚合逻辑
- 建议返回字段：
  - `totalMembers`
  - `totalRevenue`
  - `totalPoints`
  - `totalIssuedCoupons`
  - `totalUsedCoupons`
  - `recentRecords`
  - `lineData`
  - `pieData`

## 2. 生命周期历史快照接口

- 当前已存在：`GET /api/v1/analysis/lifecycle`
- 当前前端已实现能力：
  - 默认查看全部时期趋势
  - 支持按月筛选“新增会员趋势”
- 当前仍缺失内容：
  - 某一个历史月份对应的 `activeCount`
  - 某一个历史月份对应的 `lostCount`
  - 某一个历史月份对应的 `silentCount`
- 当前前端处理方式：
  - 月份切换只影响趋势和所选时期新增人数
  - 活跃/沉睡/流失仍使用后端当前快照
- 建议补充方案之一：
  - 扩展 `GET /api/v1/analysis/lifecycle`，增加 `month=YYYY-MM`
  - 或新增 `GET /api/v1/analysis/lifecycle/snapshot`
- 建议返回字段：
  - `month`
  - `activeCount`
  - `lostCount`
  - `silentCount`
  - `totalMembers`

## 3. 消费偏好雷达图接口

- 当前已存在：
  - `GET /api/v1/analysis/behavior/order-amount`
  - `GET /api/v1/analysis/behavior/repurchase`
  - `GET /api/v1/analysis/behavior/time-distribution`
  - `GET /api/v1/analysis/behavior/level-count`
  - `GET /api/v1/analysis/behavior/daily-consume`
- 当前仍缺失：
  - 消费偏好雷达图所需的画像数据
- 当前前端处理方式：
  - 雷达图继续使用前端预留数据，以保持原分析页逻辑不变
- 建议补充接口：
  - `GET /api/v1/analysis/behavior/preference-radar`
- 建议返回字段：
  - `male: number[]`
  - `female: number[]`

---

## 已补齐项

以下内容在当前版本中已经可以由现有接口支撑，前端已同步接入：

- RFM 已支持“全部时期 / 指定月份”查看，基于：
  - `GET /api/v1/members`
  - `GET /api/v1/consumptions`
- RFM 当前无需新增接口缺口
- 管理员角色回显：`GET /api/v1/admins/{id}/roles`
- 角色权限回显：`GET /api/v1/roles/{id}/permissions`
- 消费记录联表字段：`memberName`、`storeName`
- 会员优惠券联表字段：`memberName`、`couponName`
- 工作台可复用分析接口：
  - `GET /api/v1/analysis/behavior/level-count`
  - `GET /api/v1/analysis/behavior/daily-consume`
